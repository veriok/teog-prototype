# TEOG prototype

## Dependencies

- [Node.js](https://nodejs.org/) (for `npx serve`)

## Run Locally

```bash
cd the-echoes-of-germolles-idle-rpg-prototype
npx serve .
``` 

Then open http://localhost:3000 in your browser.